{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Evento creation</h1>

    {{ form(form) }}

    <ul class="record_actions">
    <li>
        <a href="{{ path('evento') }}">
            Back to the list
        </a>
    </li>
        <li>{{ render(controller('SeminarioBundle:Seminario:index')) }}</li>

    </ul>
{% endblock %}
