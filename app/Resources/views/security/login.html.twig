{# app/Resources/views/Security/login.html.twig #}
{% extends 'SeminarioBundle::layout.html.twig' %}
{# ... you will probably extends your base template, like base.html.twig #}

{% block body %}
    <br><br>

    {% if error %}
        <div>{{ error.messageKey|trans(error.messageData) }}</div>
    {% endif %}
    <div class="container">
        <form action="{{ path('login_check') }}" method="post" class="form-signin">
            <h2 class="form-signin-heading">Iniciar Sesión</h2>
            <input type="text" id="username" name="_username" value="{{ last_username }}" class="form-control" placeholder="User" required>
            <input type="password" id="inputPassword" name="_password" class="form-control" placeholder="Password" required>
            {#
                If you want to control the URL the user
                is redirected to on success (more details below)
                <input type="hidden" name="_target_path" value="/account" />
            #}
            <button class="btn btn-lg btn-primary btn-block" type="submit">login</button>
        </form>
    </div>

{% endblock %}
